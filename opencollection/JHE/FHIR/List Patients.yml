info:
  name: List Patients
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/fhir/r5/Patient?identifier=http://app.medplum.com|fhir-1234abc"
  params:
    - name: _has:Group:member:_id
      value: "30001"
      type: query
      disabled: true
    - name: identifier
      value: http://app.medplum.com|fhir-1234abc
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
