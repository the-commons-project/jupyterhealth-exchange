info:
  name: List Observations for Org
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/FHIR/R5/Observation"
  params:
  - name: patient._has:Group:member:_id
    value: "{{STUDY_ID}}"
    type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
