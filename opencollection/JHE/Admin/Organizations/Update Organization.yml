info:
  name: Update Organization
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{BASE_URL}}/api/v1/organizations/{{ORG_ID}}"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "name": "Lee Lab Updated"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
