info:
  name: Organization Studies
  type: http
  seq: 9

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/organizations/{{ORG_ID}}/studies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
