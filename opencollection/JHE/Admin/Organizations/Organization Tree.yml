info:
  name: Organization Tree
  type: http
  seq: 8

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/organizations/{{ORG_ID}}/tree"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
