info:
  name: Orgnization Details
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/organizations/20002"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
