info:
  name: Add Organization User
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/organizations/20005/user"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "user_id": 6
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
