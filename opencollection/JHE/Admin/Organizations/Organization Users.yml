info:
  name: Organization Users
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/organizations/{{ORG_ID}}/users?pageSize=1"
  params:
  - name: pageSize
    value: "1"
    type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
