info:
  name: Organization Types
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/organizations/types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
