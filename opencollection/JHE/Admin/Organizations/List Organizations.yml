info:
  name: List Organizations
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/organizations"
  params:
  - name: pageSize
    value: "5"
    type: query
  - name: page
    value: "1"
    type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
