info:
  name: Study Data Sources
  type: http
  seq: 9

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/studies/{{STUDY_ID}}/data_sources"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
