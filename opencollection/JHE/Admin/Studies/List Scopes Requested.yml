info:
  name: List Scopes Requested
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/studies/5/scope_requests"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
