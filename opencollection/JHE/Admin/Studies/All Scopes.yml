info:
  name: All Scopes
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/studies/all_scopes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
