info:
  name: List Studies
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/studies?pageSize=1"
  params:
  - name: pageSize
    value: "1"
    type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
