info:
  name: Study Clients
  type: http
  seq: 8

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/studies/{{STUDY_ID}}/clients"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
