info:
  name: Add Patients To Study
  type: http
  seq: 6

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/studies/5/patients"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "patient_ids": [12,11]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
