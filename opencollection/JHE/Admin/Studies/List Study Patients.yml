info:
  name: List Study Patients
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/studies/{{STUDY_ID}}/patients"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
