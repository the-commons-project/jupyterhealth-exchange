info:
  name: Study Scope Requests
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/studies/{{STUDY_ID}}/scope_requests"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
