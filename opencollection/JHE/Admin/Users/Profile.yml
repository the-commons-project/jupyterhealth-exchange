info:
  name: Profile
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/users/profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
