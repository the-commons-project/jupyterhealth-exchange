info:
  name: Update Consents
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{BASE_URL}}/api/v1/users/10007/consents"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "study_scope_consents": [
              {
                  "study_id": 5,
                  "scope_consents": [
                      {
                          "scope_code_id": 50002,
                          "consented": true
                      }
                  ]
              }
          ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
