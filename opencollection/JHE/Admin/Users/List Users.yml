info:
  name: List Users
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/users?pageSize=2"
  params:
  - name: pageSize
    value: "2"
    type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
