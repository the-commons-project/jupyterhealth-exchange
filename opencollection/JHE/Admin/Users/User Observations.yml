info:
  name: User Observations
  type: http
  seq: 8

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/users/10005/observations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
