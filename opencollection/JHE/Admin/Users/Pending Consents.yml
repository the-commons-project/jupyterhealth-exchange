info:
  name: Pending Consents
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/users/10007/pending_consents"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
