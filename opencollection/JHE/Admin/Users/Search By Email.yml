info:
  name: Search By Email
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/users/search_by_email"
  params:
  - name: email
    value: admin@admin.com
    type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
