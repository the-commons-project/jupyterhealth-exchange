info:
  name: Consented Scopes
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/users/10007/consented_scopes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
