info:
  name: Delete Patient
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BASE_URL}}/api/v1/patients/6"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "name": "Department or Urology",
          "type": "dept",
          "part_of": 20001
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
