info:
  name: Delete Patient
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BASE_URL}}/api/v1/patients/{{PATIENT_ID}}?organization_id={{ORG_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
