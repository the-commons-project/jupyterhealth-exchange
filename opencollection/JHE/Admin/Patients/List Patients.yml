info:
  name: List Patients
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/patients?pageSize=1"
  params:
  - name: pageSize
    value: "1"
    type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
