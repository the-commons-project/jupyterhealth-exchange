info:
  name: List Consents
  type: http
  seq: 10

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/patients/{{PATIENT_ID}}/consents"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
