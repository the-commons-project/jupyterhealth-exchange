info:
  name: Global Lookup
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/patients/global_lookup"
  params:
  - name: email
    value: patient@example.com
    type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
