info:
  name: List Practitioners
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/practitioners"
  params:
  - name: pageSize
    value: "10"
    type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
