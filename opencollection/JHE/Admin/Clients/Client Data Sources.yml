info:
  name: Client Data Sources
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/clients/{{CLIENT_APP_ID}}/data_sources"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
