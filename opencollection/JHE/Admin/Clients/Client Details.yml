info:
  name: Client Details
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/clients/{{CLIENT_APP_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
