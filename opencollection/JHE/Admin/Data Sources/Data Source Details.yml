info:
  name: Data Source Details
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/data_sources/{{DATA_SOURCE_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
