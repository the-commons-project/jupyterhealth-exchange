info:
  name: Supported Scopes
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/data_sources/{{DATA_SOURCE_ID}}/supported_scopes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
