DEBUG=True # Static assest will not be served locally eithout this set to True; set to False in production
SITE_URL="http://localhost:8000"

DB_NAME="jhe_dev"
DB_USER="jheuser"
DB_PASSWORD="jhepassword"
DB_HOST="localhost" # Use host.docker.internal if connecting to localhost from Docker
DB_PORT=5432

OIDC_RSA_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMII... <output of: $ openssl genrsa -out oidc.key 4096> ...H67\n-----END PRIVATE KEY-----"
SAML2_ENABLED=0

#  if unset a randomly generated value is used which will not work with more than one worker
SECRET_KEY=

# Advanced configuration used to bootstrap users into specific organizations
# PRACTITIONER_DEFAULT_ORGS="20001:viewer;20002:manager" # "<org id>:<role>;..." role=manager|member|viewer

#  ==== TBD: Move all below to get_settings ===

TIME_ZONE="America/Los_Angeles"
REGISTRATION_INVITE_CODE="jhe"
SSO_VALID_DOMAINS="example.com,example.org"
IDENTITY_PROVIDER_METADATA_URL="https://mocksaml.com/api/saml/metadata"

# Pipeline mode: "normalized" (read from OW API) or "raw" (read from MinIO S3)
OW_PIPELINE_MODE=normalized

# MinIO settings (only used when OW_PIPELINE_MODE=raw)
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
